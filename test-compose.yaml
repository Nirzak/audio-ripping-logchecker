services:
  logchecker-test:
    image: ghcr.io/nirzak/audio-ripping-logchecker:test
    container_name: logchecker-test
    build: .
    environment:
      - PUID=1002
      - PGID=1002
      - SUBPATH=/logchecker-test
      - LOG_LEVEL=debug
      - RATE_LIMIT_RPS=16
      - RATE_LIMIT_BURST=10
    ports:
      - "5051:5050"
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
